    // 移除环境
    delete environments[envId];
    
    res.json({ success: true, message: `环境 ${envId} 已停止` });
  } catch (error) {
    console.error(`[${new Date().toLocaleTimeString()}] 停止环境出错:`, error);
    res.json({ success: false, message: `停止环境出错: ${error.message}` });
  }
});

// 元素探索器页面路由
